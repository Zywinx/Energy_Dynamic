# ECLAM-SB 设计记录

## Baseline

CLAM-SB + instance-level clustering。

## 改进 1：slide-level energy uncertainty

\[
E(S) = -T \log \sum_c \exp(f_c(S)/T)
\]

Energy 只用于 slide-level uncertainty / reliability 评估，不引入 unknown 类。

## 改进 2：dynamic instance loss

原始：

\[
L = 0.7 L_{slide} + 0.3 L_{inst}
\]

新版：

\[
L = L_{slide} + \lambda(t) L_{inst}
\]

`bag_weight=0.7` 时，等价 `lambda=(1-0.7)/0.7≈0.4286`。

## 为什么不做 unknown 三分类

当前没有真实 unknown slide label 或 patch label。low-attention patch 不等价于 unknown，只能称为 weak negative evidence 或 non-diagnostic candidate。

## 当前可发表性定位

这是 reliability-aware CLAM extension：保留 CLAM 弱监督定位能力，用 energy 评估 slide-level 可信度，用 dynamic instance loss 降低早期 noisy pseudo-label 干扰，并支持主动学习候选病例排序。
